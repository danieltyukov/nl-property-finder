## What this changes

<!-- What the change is and why it is right. One or two paragraphs. -->

## How it was checked

<!--
Which of these you ran, and anything you tried by hand. CI runs them on this
pull request as well.

  npm run lint
  npm run typecheck
  npm test
  npm run test:integration
  npm run test:e2e

For a new or changed source: the fixture was recorded with
`npm run record -- <id>` and `nlpf sources test <id>` passed against the live
site once. Tests never touch the live site.
-->

## Notes

<!--
Anything a reviewer would otherwise have to work out: a change to the config
schema and whether old config files still load, a new dependency and why it
earns its place, a change to what leaves the machine.

No emojis, and no em or en dashes, in code, comments, documentation or the
commit message.
-->
