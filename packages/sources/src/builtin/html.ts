import type { SourceAdapter } from '@nlpf/core';

/** Task 6b: MVGM, Stadswonen, WoningNet DAK, paid aggregators, small agents. Owned by that task; returns every adapter in the group. */
export function htmlAdapters(): SourceAdapter[] {
  return [];
}
