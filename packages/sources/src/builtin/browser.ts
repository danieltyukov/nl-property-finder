import type { SourceAdapter } from '@nlpf/core';

/** Task 6a: Pararius, Huurwoningen, Kamer.nl, Holland2Stay, Xior. Owned by that task; returns every adapter in the group. */
export function browserAdapters(): SourceAdapter[] {
  return [];
}
