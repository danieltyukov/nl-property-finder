import type { SourceAdapter } from '@nlpf/core';

/** Task 5b: Zig portals, Embrace (Woonnet Rijnmond), OGonline agencies. Owned by that task; returns every adapter in the group. */
export function portalAdapters(): SourceAdapter[] {
  return [];
}
